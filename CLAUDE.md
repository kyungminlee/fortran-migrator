# CLAUDE.md

## Workspace

This project has a `workspace/` directory for cross-session working notes (tasks, plans, distilled docs). It is its **own git repo**, versioned separately from this project — don't commit it into the project's history, and don't `git push` it. See `workspace/README.md` for the conventions on how to use and maintain it.
