name: multifloats

types:
  real: "TYPE(real64x2)"
  complex: "TYPE(cmplx64x2)"

literals:
  mode: constructor
  kind_suffix: null
  real_constructor: real64x2
  complex_constructor: cmplx64x2

intrinsics:
  mode: wrap_constructor

prefixes:  # only the R (real) and C (complex) families are remapped
  R: M
  C: W

module:
  name: multifloats
  type_names: [real64x2, cmplx64x2]
  constant_names:
    - dd_zero
    - dd_one
    - dd_two
    - dd_half
    - dd_eight
    - dd_safmin
    - dd_safmax
    - dd_tsml
    - dd_tbig
    - dd_ssml
    - dd_sbig
    - dd_rtmin
    - dd_rtmax
    - dd_to_double
    - dd_real
  generic_names:
    - abs
    - sqrt
    - sin
    - cos
    - tan
    - exp
    - log
    - log10
    - atan
    - asin
    - acos
    - aint
    - anint
    - sinh
    - cosh
    - tanh
    - asinh
    - acosh
    - atanh
    - erf
    - erfc
    - erfc_scaled
    - gamma
    - log_gamma
    - bessel_j0
    - bessel_j1
    - bessel_y0
    - bessel_y1
    - bessel_jn
    - bessel_yn
    - fraction
    - rrspacing
    - spacing
    - epsilon
    - huge
    - tiny
    - aimag
    - conjg
    - sign
    - mod
    - atan2
    - dim
    - modulo
    - hypot
    - nearest
    - min
    - max
    - maxval
    - minval
    - maxloc
    - minloc
    - sum
    - product
    - dot_product
    - norm2
    - findloc
    - matmul
    - digits
    - maxexponent
    - minexponent
    - radix
    - precision
    - range
    - exponent
    - dble
    - int
    - nint
    - real
    - cmplx
    - ceiling
    - floor
    - scale
    - set_exponent
    - storage_size
    - random_number
  operator_generics:
    - "operator(+)"
    - "operator(-)"
    - "operator(*)"
    - "operator(/)"
    - "operator(**)"
    - "operator(==)"
    - "operator(/=)"
    - "operator(<)"
    - "operator(>)"
    - "operator(<=)"
    - "operator(>=)"
    - "assignment(=)"

known_constants:
  ZERO: DD_ZERO
  ONE: DD_ONE
  TWO: DD_TWO
  HALF: DD_HALF
  EIGHT: DD_EIGHT

la_constants_map:
  zero: DD_ZERO
  half: DD_HALF
  one: DD_ONE
  two: DD_TWO
  eight: DD_EIGHT
  safmin: DD_SAFMIN
  safmax: DD_SAFMAX
  tsml: DD_TSML
  tbig: DD_TBIG
  ssml: DD_SSML
  sbig: DD_SBIG

la_constants_suffix: _MW

# Extra names for the free-form PARAMETER-nuking pass, merged over
# la_constants_map. Neither is an LA_CONSTANTS member: czero is
# declared alongside zero in the Pattern A files, and dnrm2.f90's
# local ``maxN = huge(0.0_wp)`` is equivalent to DD_SAFMAX (itself
# huge(0.0_dp) packed into float64x2's high limb).
#
# rtmin/rtmax are deliberately absent from both maps: in several
# LAPACK routines they are local variables computed at runtime, not
# PARAMETERs.
param_nuke_extra:
  czero: DD_ZERO
  maxn: DD_SAFMAX

c_interop:
  # Upstream multifloats C names (no _t suffix; complex POD with
  # .re/.im members of float64x2). The C++ bridge re-exports these as
  # operator-rich aliases of multifloats::float64x2 / complex64x2.
  real_type: float64x2
  complex_type: complex64x2
  c_real_type: float64x2
  mpi_real: MPI_FLOAT64X2
  mpi_complex: MPI_COMPLEX64X2
  mpi_sum_real: MPI_MM_SUM
  mpi_sum_complex: MPI_WW_SUM
  # MUMPS issues MPI_Allreduce(..., MPI_FLOAT64X2, MPI_MAX, ...) when
  # collecting per-rank scaling extrema; stock MPI_MAX is undefined for
  # the user-defined datatype. The bridge ships AMX/AMN ops; MIN is
  # listed for symmetry though MUMPS only actually uses MAX today.
  mpi_max_real: MPI_MM_AMX
  mpi_max_complex: MPI_WW_AMX
  mpi_min_real: MPI_MM_AMN
  mpi_min_complex: MPI_WW_AMN
  # Fortran module (bind-c) that surfaces the custom datatype + op
  # handles above to migrated MUMPS. Was formerly hardcoded in the
  # migrator; now target-driven so KIND targets (kind16 -> quad_mpi_f)
  # can supply their own support module the same way.
  mpi_module: multifloats_mpi_f
  needs_mpi_check: false
  header_mode: include
  header: multifloats_bridge.h
