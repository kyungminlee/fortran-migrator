# tests/pblas — TODO

Resolved items have moved to `CHANGELOG.md`. No live items.

The 6 auxiliary `[dz]agemv` / `[dz]ahemv` / `[dz]asymv` / `[dz]atrmv`
norm stems plus the rest of the 61-row PBLAS surface are tested; runs
clean on all three targets under the `linux-impi` preset.
