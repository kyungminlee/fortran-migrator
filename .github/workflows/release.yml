name: Release Libraries

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag (e.g. v0.2.0)'
        required: false

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  # ── Stage migrated sources (one per target) ──────────────────────
  stage:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [kind16, kind10, multifloats]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Extract external sources
        run: |
          cd external
          [ -d lapack-3.12.1 ] || tar xzf lapack-3.12.1.tar.gz
          [ -d scalapack-2.2.3 ] || tar xzf scalapack-2.2.3.tar.gz

      - name: Stage ${{ matrix.target }}
        run: |
          cd src
          uv run python -m pyengine stage \
            ${{ github.workspace }}/staging-${{ matrix.target }} \
            --target ${{ matrix.target }}

      - name: Convergence report
        run: |
          cd src
          RECIPES=(blas blacs lapack ptzblas pbblas pblas scalapack scalapack_c)
          {
            echo "## Convergence report: \`${{ matrix.target }}\`"
            echo ""
            for lib in "${RECIPES[@]}"; do
              RECIPE="../recipes/${lib}.yaml"
              OUTDIR="${{ github.workspace }}/staging-${{ matrix.target }}/${lib}/src"
              if [ ! -d "$OUTDIR" ]; then
                continue
              fi
              echo "### ${lib}"
              echo '```'
              uv run python -m pyengine converge "$RECIPE" "$OUTDIR" \
                --target ${{ matrix.target }} --full 2>&1 || true
              echo '```'
              echo ""
            done
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload staged sources
        uses: actions/upload-artifact@v4
        with:
          name: staging-${{ matrix.target }}
          path: staging-${{ matrix.target }}/
          retention-days: 1

  # ── Build matrix: target x compiler x MPI ────────────────────────
  build:
    needs: stage
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [kind16, kind10, multifloats]
        compiler:
          - { name: gfortran-12,  fc: gfortran-12,   cc: gcc-12,    cxx: g++-12,      pkg: gcc-12 g++-12 gfortran-12 }
          - { name: gfortran-15,  fc: gfortran-15,   cc: gcc-15,    cxx: g++-15,      pkg: gcc-15 g++-15 gfortran-15 }
          - { name: ifx-2024,     fc: ifx,            cc: icx,       cxx: icpx,        pkg: "",  oneapi: "2024.2" }
          - { name: ifx-2025,     fc: ifx,            cc: icx,       cxx: icpx,        pkg: "",  oneapi: "2025.1" }
          - { name: flang-20,     fc: flang-new-20,   cc: clang-20,  cxx: clang++-20,  pkg: "" }
        mpi:
          - { name: openmpi,  pkg: libopenmpi-dev }
          - { name: mpich,    pkg: libmpich-dev }
          - { name: intelmpi }
        exclude:
          # ifx on Linux/x86-64 does not implement REAL(KIND=10)
          # (80-bit x87 extended), so the kind10 target cannot be
          # built with Intel's compiler.
          - target: kind10
            compiler: { name: ifx-2024 }
          - target: kind10
            compiler: { name: ifx-2025 }
          # LLVM Flang 20 has no REAL(KIND=16) (IEEE binary128) support
          # yet, so it cannot build the kind16 target. It *does*
          # support REAL(KIND=10), so kind10 and multifloats are OK.
          - target: kind16
            compiler: { name: flang-20 }

    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            cmake/

      - name: Download staged sources
        uses: actions/download-artifact@v4
        with:
          name: staging-${{ matrix.target }}
          path: staging/

      - name: Install GCC (gfortran)
        if: startsWith(matrix.compiler.name, 'gfortran')
        run: |
          sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.compiler.pkg }}

      - name: Install Intel oneAPI compilers
        if: startsWith(matrix.compiler.name, 'ifx')
        run: |
          wget -O- https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB \
            | gpg --dearmor | sudo tee /usr/share/keyrings/oneapi-archive-keyring.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/oneapi-archive-keyring.gpg] https://apt.repos.intel.com/oneapi all main" \
            | sudo tee /etc/apt/sources.list.d/oneAPI.list
          sudo apt-get update
          YEAR=$(echo "${{ matrix.compiler.oneapi }}" | cut -d. -f1)
          MINOR=$(echo "${{ matrix.compiler.oneapi }}" | cut -d. -f2)
          sudo apt-get install -y \
            intel-oneapi-compiler-fortran-${YEAR}.${MINOR} \
            intel-oneapi-compiler-dpcpp-cpp-${YEAR}.${MINOR}

      - name: Install LLVM Flang 20
        if: matrix.compiler.name == 'flang-20'
        run: |
          wget -O- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
          sudo add-apt-repository -y "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-20 main"
          sudo apt-get update
          sudo apt-get install -y flang-20 clang-20 libomp-20-dev

      - name: Install OpenMPI
        if: matrix.mpi.name == 'openmpi'
        run: sudo apt-get install -y libopenmpi-dev

      - name: Install MPICH
        if: matrix.mpi.name == 'mpich'
        run: sudo apt-get install -y libmpich-dev

      - name: Install Intel MPI
        if: matrix.mpi.name == 'intelmpi' && !startsWith(matrix.compiler.name, 'ifx')
        run: |
          wget -O- https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB \
            | gpg --dearmor | sudo tee /usr/share/keyrings/oneapi-archive-keyring.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/oneapi-archive-keyring.gpg] https://apt.repos.intel.com/oneapi all main" \
            | sudo tee /etc/apt/sources.list.d/oneAPI.list
          sudo apt-get update
          sudo apt-get install -y intel-oneapi-mpi-devel

      - name: Install Intel MPI (with oneAPI compilers)
        if: matrix.mpi.name == 'intelmpi' && startsWith(matrix.compiler.name, 'ifx')
        run: |
          sudo apt-get install -y intel-oneapi-mpi-devel

      - name: Install fypp (needed by multifloats)
        if: matrix.target == 'multifloats'
        run: pip install fypp

      - name: Configure
        run: |
          # Source Intel oneAPI environment if needed
          if [ -n "${{ matrix.compiler.oneapi }}" ] || [ "${{ matrix.mpi.name }}" = "intelmpi" ]; then
            source /opt/intel/oneapi/setvars.sh
          fi
          # For Intel MPI with non-Intel compilers, set underlying compiler
          if [ "${{ matrix.mpi.name }}" = "intelmpi" ] && [ -z "${{ matrix.compiler.oneapi }}" ]; then
            export I_MPI_F90=${{ matrix.compiler.fc }}
            export I_MPI_CC=${{ matrix.compiler.cc }}
            export I_MPI_CXX=${{ matrix.compiler.cxx }}
          fi
          cmake -S staging -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_Fortran_COMPILER=${{ matrix.compiler.fc }} \
            -DCMAKE_C_COMPILER=${{ matrix.compiler.cc }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.compiler.cxx }} \
            -DCMAKE_INSTALL_PREFIX=${{ github.workspace }}/install

      - name: Build
        run: |
          if [ -n "${{ matrix.compiler.oneapi }}" ] || [ "${{ matrix.mpi.name }}" = "intelmpi" ]; then
            source /opt/intel/oneapi/setvars.sh
          fi
          if [ "${{ matrix.mpi.name }}" = "intelmpi" ] && [ -z "${{ matrix.compiler.oneapi }}" ]; then
            export I_MPI_F90=${{ matrix.compiler.fc }}
            export I_MPI_CC=${{ matrix.compiler.cc }}
            export I_MPI_CXX=${{ matrix.compiler.cxx }}
          fi
          cmake --build build -j$(nproc)

      - name: Install
        run: |
          if [ -n "${{ matrix.compiler.oneapi }}" ] || [ "${{ matrix.mpi.name }}" = "intelmpi" ]; then
            source /opt/intel/oneapi/setvars.sh
          fi
          cmake --install build

      - name: Package
        run: |
          cd install
          ARCHIVE="${{ matrix.target }}-${{ matrix.compiler.name }}-${{ matrix.mpi.name }}-linux-x86_64"
          tar czf ${{ github.workspace }}/${ARCHIVE}.tar.gz .
          echo "ARCHIVE=${ARCHIVE}" >> $GITHUB_ENV

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}.tar.gz
          retention-days: 7

  # ── Create GitHub Release ────────────────────────────────────────
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          pattern: '*-*-*-linux-x86_64'

      - name: Collect tarballs
        run: |
          mkdir -p release
          find artifacts -name '*.tar.gz' -exec cp {} release/ \;
          ls -la release/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.tar.gz
          generate_release_notes: true
          body: |
            ## Extended-precision numerical libraries

            Pre-built static libraries for Linux x86-64.

            ### Targets
            | Target | Description | Prefix |
            |--------|-------------|--------|
            | `kind16` | 128-bit quad precision (`REAL(KIND=16)`) | Q/X |
            | `kind10` | 80-bit extended precision (`REAL(KIND=10)`) | E/Y |
            | `multifloats` | Double-double (`float64x2`) | DD/ZZ |

            ### Compilers
            | Compiler | Tag |
            |----------|-----|
            | GCC/gfortran 12 | `gfortran-12` |
            | GCC/gfortran 15 | `gfortran-15` |
            | Intel oneAPI 2024 (ifx) | `ifx-2024` |
            | Intel oneAPI 2025 (ifx) | `ifx-2025` |
            | LLVM Flang 20 | `flang-20` (kind10 and multifloats only) |

            ### MPI Implementations
            MPI-dependent libraries (BLACS, PBLAS, PBBLAS, ScaLAPACK, ScaLAPACK_C)
            are built with each MPI implementation:

            | MPI | Tag |
            |-----|-----|
            | OpenMPI | `openmpi` |
            | MPICH | `mpich` |
            | Intel MPI | `intelmpi` |

            ### Installation
            ```bash
            tar xzf kind16-gfortran-15-openmpi-linux-x86_64.tar.gz -C /usr/local
            ```

            Libraries are compiler-tagged (e.g. `libqblas-gfortran-15.a`).
            Fortran modules are in `lib/fmod/<mod-tag>/`.
