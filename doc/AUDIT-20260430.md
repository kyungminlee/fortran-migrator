# Fortran-migrator audit — 2026-04-30

Snapshot of project health at HEAD `823e838` on branch `tests`.
Compiled from three parallel exploration passes (engine + recipes,
tests + coverage, build + docs + branches + upstream) plus targeted
verification. Each section cites file paths so a future reader can
re-derive any claim.

---

## Executive summary

**Overall verdict: HEALTHY.** The migrator is in a mature steady state.
The engine is clean, recipes are well-structured, tests cover all
ten library suites, three precision targets build end-to-end, and
documentation is mostly fresh after this session's prefix-name
refresh. The only identifiable correctness gap (XBLAS rename across
non-position-0 prefixes) was closed in commit `9938731` earlier this
session, and the most prominent test failure (Phase P22 zgedmd
fp64-leak) was traced to the **reference** build, not the migrator,
and fixed via `tests/lapack/reflapack/overrides/zgedmd.f90`
(commit `36f6f24`).

**Top issues / risks (ranked):**

1. ~~**CI never runs ctest.**~~ **RESOLVED** in commit `35e6ce0`:
   added a smoke-test job (kind10 + gfortran-15 + openmpi) to
   `.github/workflows/release.yml` that runs full ctest after build.
   `release` job now needs both `build` and `test`. Other (target ×
   compiler × MPI) combos can be added as their pass rates stabilize.
2. ~~**`doc/PROCEDURES.md` prefix table is stale.**~~ **RESOLVED**
   in commit `35e6ce0`: `tools/gen_procedures.py`'s hardcoded
   `t/v/tv/vt/it/iv` for multifloats updated to `m/w/mw/wm/im/iw`,
   and `doc/PROCEDURES.md` regenerated against fresh staging trees.
3. ~~**Five test failures across all three targets, newly discovered**~~
   **RESOLVED** (Addenda 2 & 3, commits `4b2d5ee` + `50adda9`): four
   were test-driver bugs (CABS1 vs Euclidean, BLACS_GET WHAT=10
   semantics) and one was an upstream ScaLAPACK PBLAS bug
   (`p[dz]atrmv_.c` L,T branch dropped ALPHA), patched via
   `recipes/pblas/source_overrides/`. Final ctest pass rates:
   100.00% across all three targets (1021/1021/1044), excluding
   only the unrelated `test_pzheevr` Intel-MPI sandbox issue
   (Addendum 4).
4. ~~**9 stale doc references to T/V or DD/ZZ**~~ **RESOLVED**:
   823e838 closed the prefix references; `PROCEDURES.md` regenerated
   in `35e6ce0`; `INTRINSICS.md` cross-reference appendix added in
   `bc848ac`. No residual stale prefix docs.
5. ~~**3 abandoned remote `feature/*` branches**~~ **RESOLVED**:
   `feature/coalesce` and `feature/multifloats` deleted (fully
   merged); `feature/fuzz` was already gone upstream — `git remote
   prune` caught up.
6. **`tests/blas/` has 76 test files but 0 dedicated drivers per
   the BLAS routine list** — BLAS coverage is transitive only
   through LAPACK/PBLAS callers. Acceptable, but worth a
   docs note.
7. **One vendored upstream bug** (ScaLAPACK 2.2.3 `p?lanhs.f`
   NPROW=1 norm underestimate) is documented and worked around
   but never reported upstream.
8. **Engine `__main__.py` `cmd_stage` is 305 lines** and
   `_generate_cmake` is 233 lines. Both are coherent (template
   generation / orchestration), but split-worthy if they grow.
9. **Phase-22 DMD precision** — closed by reference-side override
   (the bug was in `gfortran -freal-8-real-16` truncating
   `CMPLX(real, 0, KIND=8)` even with KIND=16 storage; the
   migrator's xgedmd was always correct). Documenting this for
   posterity.

**Quick stats** (verified at HEAD `823e838`):

| Metric | Value | Source |
|---|---|---|
| Engine LOC | 9,039 | `wc -l src/pyengine/*.py` |
| Engine modules | 11 (+ `__init__.py`) | `src/pyengine/` |
| Recipes | 11 | `recipes/*.yaml` |
| Targets | 3 | `targets/*.yaml` |
| Vendored externals | 7 (LAPACK 3.12.1, ScaLAPACK 2.2.3, MUMPS 5.8.2, XBLAS 1.0.248, multifloats, multifloats-mpi, impi-headers) | `external/` |
| Test files (Fortran) | 1,051 across 10 suites | `find tests -name "test_*.f90"` |
| Test files (C) | 3 (mumps only) | `find tests -name "test_*.c"` |
| ctest pass rate, kind16 | 1011/1013 (99.8%) | `/tmp/stg-q-fix/build/Testing/Temporary/LastTestsFailed.log` |
| ctest pass rate, kind10 | 728/728 (100.0%) | `/tmp/stg-e10-fix/build/Testing/Temporary/LastTestsFailed.log` |
| ctest pass rate, multifloats | ~1010/1013 (99.7%) | `/tmp/stg-mf-fix/build/Testing/Temporary/LastTestsFailed.log` |
| Active local branches | 12 | `git branch` |
| Stale remote branches (>30 days unmerged) | 1 (`feature/fuzz`, 42 days) | `git for-each-ref` |
| Open upstream bugs | 1 (`p?lanhs` NPROW=1) | `doc/UPSTREAM_BUGS.md` |

---

## 1. Engine (`src/pyengine/`)

### 1.1 Per-module health

| Module | LOC | Health | Notes |
|---|---|---|---|
| `fortran_migrator.py` | 2842 | clean | Core type/literal/intrinsic/routine-name rewriter. Hybrid Flang+gfortran-parsetree-guided + regex fallback. No `TODO/FIXME/HACK` markers in source. |
| `pipeline.py` | 1365 | clean | Migration orchestration; `_apply_extra_renames` + collection wiring sit here. Imports from 6 sibling modules — coupling is justified by orchestration role. |
| `c_migrator.py` | 1648 | clean | C clone + type-substitution path used by BLACS/PBLAS/XBLAS. Multi-char-prefix expansion (D→DD historical; now M) and case-preserving renames live in `_make_rename_substituter` (lines 682-749). |
| `__main__.py` | 1148 | clean | CLI entry. Two large functions: `_generate_cmake` (233 lines, lines 316-548) and `cmd_stage` (305 lines, lines 728-1032). Both are coherent (template generation / 10-library orchestration). |
| `prefix_classifier.py` | 372 | clean | `PrecisionSlot` / `PrecisionFamily` / `SymbolClassification` dataclasses. `_find_families_single_pass` (lines 215-280) iterates **every** candidate position for precision letters — the reason commit `9938731` works. |
| `intrinsics.py` | 352 | clean (data-only) | Lookup tables for type-specific → generic intrinsic rewrites. Pure data; not auto-derived from `doc/INTRINSICS.md`. |
| `flang_parser.py` | 360 | clean | `flang-new -fc1 -fdebug-dump-parse-tree` JSON wrapper. |
| `gfortran_parser.py` | 350 | clean | gfortran tree-dump fallback. |
| `config.py` | 272 | clean | YAML recipe loader. `RecipeConfig` dataclass with ~25 fields. Defensive `import yaml` (lines 11-14). |
| `symbol_scanner.py` | 175 | clean | Symbol discovery via Flang/gfortran/regex fallback or `nm` over compiled archives. |
| `target_mode.py` | 153 | clean | `TargetMode` dataclass; loads `targets/*.yaml`. |
| `__init__.py` | 2 | n/a | Package marker. |

**Markers found:** zero `TODO`/`FIXME`/`HACK`/`XXX`/`KLUDGE` in active code. The only `TODO`-like reference is a doc-string mention of `tests/mumps/TODO.md` in `__main__.py:1005`.

**Dead code:** none detected by import-graph traversal.

### 1.2 Coupling

Acyclic, unidirectional:

```
__main__.py ─┬─> config
             ├─> pipeline ─┬─> symbol_scanner ─┬─> flang_parser
             │             │                    └─> gfortran_parser
             │             ├─> prefix_classifier
             │             ├─> fortran_migrator ─> {target_mode, intrinsics, *_parser, prefix_classifier}
             │             └─> c_migrator ─> {target_mode, prefix_classifier}
             ├─> prefix_classifier
             ├─> symbol_scanner
             └─> target_mode
```

Lower-level modules (`intrinsics`, `target_mode`, `symbol_scanner`)
have no upward dependencies. Higher-level modules (`pipeline`,
`__main__`) properly orchestrate. **No circular imports.**

### 1.3 Migrated-tree health spot-checks

Performed against `/tmp/stg-q-fix`, `/tmp/stg-e10-fix`,
`/tmp/stg-mf-fix` (kind16 / kind10 / multifloats stages):

| Check | Result |
|---|---|
| Residual `DBLE(`/`DCMPLX(`/`KIND=8`/`real*8`/`double precision` in **active code** | None — all hits are in preserved comments. |
| `BLAS_DGBMV_X` / `BLAS_ZGEMV_X` / etc. survive in migrated `lapack/src/` | None on any of the three targets (post-`9938731`). 8 hits each in the read-only `_reflapack_src/` reference tree, as expected. |
| `external/lapack-3.12.1/SRC/zgedmd.f90` vs `/tmp/stg-q-fix/lapack/src/xgedmd.f90` | Only Z→X / D→Q / `WP=real64` → `WP=16` differences. Migration is clean. |

### 1.4 Notable design observations

- **The classifier is the load-bearing component.** Its
  position-agnostic family discovery is what made `9938731` a one-line
  fix. Documented behavior in `prefix_classifier.py:215-280`. Future
  recipe-level "rename pattern" features (debated in earlier session)
  are unnecessary as long as the symbol universe is correctly seeded
  via `depends:`.
- **`pipeline.py:_apply_extra_renames`** (lines 23-39) is the single
  post-classification rename hook. Three call sites (lines 868, 981,
  1112, 1298) — a sibling `pattern_renames` mechanism would mirror
  this shape, but is not currently planned.
- **C migrator's `_make_rename_substituter`** (lines 682-749) carries
  case-preservation logic for multi-char prefix expansions (e.g. the
  abandoned DD/ZZ era); now mostly idle since prefixes are
  single-character. Worth keeping for any future multi-char target.

---

## 2. Recipes & targets

### 2.1 Per-recipe status (one-line each)

| Recipe | depends | skip_files | source_overrides | extras subdirs | Status |
|---|---|---|---|---|---|
| `blas.yaml` | (none) | 4 | 2 (`dnrm2`, `dznrm2`) | `blas/source_overrides/` | clean |
| `xblas.yaml` | blas | 402 (mixed-precision variants) | 0 | `xblas/` (gen script) | large skip count justified — XBLAS is a 4× combinatorial set with most variants unreachable from LAPACK |
| `lapack.yaml` | blas, **xblas** | 28 | 0 | `lapack/extras/`, `lapack/mf_helpers/` | xblas dep added in `9938731` |
| `blacs.yaml` | (none) | 0 | 0 | `blacs/mfc_overrides/` (4 files) | clean |
| `pblas.yaml` | blas, blacs | 11 | 0 | `pblas/mfc_overrides/` (3 files) | clean |
| `pbblas.yaml` | blas, blacs | 1 | 0 | — | clean |
| `ptzblas.yaml` | blas | 1 | 0 | — | clean |
| `scalapack.yaml` | lapack, blacs, pblas | 2 | 2 (`pdlanhs`, `pzlanhs`) | `scalapack/extras/`, `scalapack/source_overrides/`, `scalapack/mfc_overrides/` | one upstream-bug workaround |
| `scalapack_c.yaml` | lapack, blacs, pblas, scalapack | 0 | 0 | — | clean |
| `mumps.yaml` | blas, lapack, scalapack | 32 | 0 | `mumps/` (keep-kind manifest); `mumps/kind10/` + `mumps/multifloats/` (per-target overrides, commit `c063db3`) | new, integration ongoing; kind16 + kind10 wired |

**Source-override sanity check:** all 4 declared override files exist
(`recipes/blas/source_overrides/{dnrm2,dznrm2}.f90`,
`recipes/scalapack/source_overrides/p{d,z}lanhs.f`). No dangling
references.

**Extra-renames sanity check:** all entries trace to a documented
reason (mostly the LAPACK gate-bug `ILAENV→ILAENV_EP`,
`PJLAENV→PJLAENV_EP`, etc., or the C/Fortran rename mirror for
`PDLAIECTB`/`PDLAIECTL`). None are pre-classifier-fix artifacts.

### 2.2 Targets (`targets/`)

| Target | Real prefix | Complex prefix | Module | Notes |
|---|---|---|---|---|
| `kind10.yaml` | E | Y | none | `REAL(KIND=10)` / `COMPLEX(KIND=10)`; literals end `_10`; intrinsics get `KIND=10` arg appended |
| `kind16.yaml` | Q | X | none | `REAL(KIND=16)` / `COMPLEX(KIND=16)`; quad; serves as differential reference |
| `multifloats.yaml` | M | W | `multifloats` | `TYPE(real64x2)` / `TYPE(cmplx64x2)`; ~60 generic-name overloads + 8 operators; custom MPI types `MPI_FLOAT64X2` etc. |

All three target YAMLs are internally consistent. M/W is the active
multifloats prefix (per `823e838`'s prefix-doc refresh).

### 2.3 Open recipe-level issues

- **`xblas.yaml` skip_files (402 entries)** is generated by
  `recipes/xblas/gen_skip_list.py`. No automated re-run check; if
  XBLAS upstream ever ships new mixed-precision variants, the script
  must be re-run by hand.
- **`mumps.yaml`** is recent and still actively gaining test
  coverage (commits `cda8772`, `64a8b97`, `d9c6f68` etc.). Recipe
  itself is stable; consumer test suite is not.

---

## 3. Test coverage

### 3.1 Per-suite inventory

| Suite | `test_*.f90` | `test_*.c` | TODO.md | Cmake-glob |
|---|---|---|---|---|
| `tests/blas` | 76 | 0 | (none) | `level{1,2,3}/test_*.f90` (CMakeLists.txt:79-85) |
| `tests/lapack` | 611 | 0 | yes | `{linear_solve,factorization,eigenvalue,svd,auxiliary}/test_*.f90` (lines 110-116) |
| `tests/scalapack` | 171 | 0 | yes | hierarchy globs |
| `tests/pblas` | 62 | 0 | yes | level1/2/3 |
| `tests/pbblas` | 9 | 0 | yes | smoke-only |
| `tests/blacs` | 22 | 0 | yes | rank-1 sandbox |
| `tests/ptzblas` | 35 | 0 | yes | mixed-prec ZZDOTC/ZZDOTU coverage |
| `tests/xblas` | 44 | 0 | yes | hand-coded quad references |
| `tests/mumps` | 21 | 3 | yes | Fortran + C parity drivers |
| `tests/common` | 0 | 0 | (none) | helper modules |
| **Totals** | **1051** | **3** | 8 suites | |

### 3.2 Phase tracker

Every Phase ID across all `tests/*/TODO.md` files, normalized:

| Phase | Suite | Status | Reason / Blocker |
|---|---|---|---|
| L1-L4 | lapack | RESOLVED | Linear solve coverage landed |
| L5 | lapack | RESOLVED | `dgesvj`/`zgesvj` Infinity on multifloats fixed by `133020a` (recipes/blas dnrm2/dznrm2 source overrides) + `8a3910e` (la_constants_mf via LAPACK formula). Verified PASS on fresh stage 2026-04-30. |
| L6-L23 | lapack | RESOLVED | Various LAPACK families |
| P6 | lapack | RESOLVED | `?gesvdq` workspace-query buffer overrun (commit `a2d290c`) |
| P7 | lapack | RESOLVED | Same (companion fix) |
| P17xx | lapack | BLOCKED | xx-suffix LAPACK iterative-refinement; needs XBLAS test infrastructure |
| P22 | lapack | RESOLVED | DMD fp64 leak via reference-build override `tests/lapack/reflapack/overrides/zgedmd.f90` (commit `36f6f24`) |
| UB-01 | engine | RESOLVED | `*.F` extension glob in pyengine source scan |
| UB-02 | engine | RESOLVED | Preprocessor-directive walker fix in PARAMETER lookahead |
| UB-03 | engine/recipes | RESOLVED | iparam2stage prefix mapping for E/Q/M/Y/X/W |
| UB-04 | c_migrator | RESOLVED | `complex*16` locals in C dispatcher copy-originals |
| UB-04b | c_migrator | RESOLVED | Scope of UB-04 type-aliases narrowed to dispatchers |
| UB-05 | tests/pblas | RESOLVED (verify-only) | Multifloats PBLAS extern "C" propagation |
| UB-06 | tests/lapack | RESOLVED | reflapack glob picks up `.F` files |
| Banded (scalapack) | scalapack | DELIVERED | All 22 banded drivers in `tests/scalapack/linear_solve/` |
| MUMPS L-1..L-5 | mumps | RESOLVED | Correctness-review polish |
| MUMPS M-1..M-2 | mumps | RESOLVED | finalize-then-cleanup, mpiseq Reduce abort |
| MUMPS B-1..B-3 | mumps | RESOLVED | Build-glue + MPI stub layering |
| MUMPS B-4 | mumps | PARTIAL | kind10 EP overrides + wrapper landed 2026-04-30 (`emumps` builds); multifloats overrides authored but build blocked by B-8 |
| MUMPS B-7 | mumps | PARTIAL | same TODO entry — kind10 share landed |
| MUMPS B-8 | mumps | OPEN | new (2026-04-30): multifloats USE-injector mishandles CPP-interspersed continuation in SUBROUTINE headers (engine bug) |
| MUMPS B-9 | mumps | OPEN | new (2026-04-30): C-bridge + test infrastructure hardcoded to kind16, blocks running mumps tests under kind10/mf |
| MUMPS G-1 | mumps | RESOLVED | Fortran/C parity tests for D and Z |
| MUMPS G-2 | mumps | PARTIAL | `target_kind10/target_mumps.fypp` landed; multifloats wrapper deferred behind B-8/B-9 |
| MUMPS D-1 | mumps | RESOLVED | error-validation finding |

**Status counts:** 25 RESOLVED / DELIVERED, 3 PARTIAL (B-4, B-7, G-2),
2 OPEN (B-8, B-9 — new this session), 1 BLOCKED (P17xx, XBLAS infra),
0 IN_PROGRESS as of 2026-04-30 (post-`c063db3`).

### 3.3 Current ctest failures (from `/tmp/stg-{q,e10,mf}-fix` builds)

**Updated 2026-04-30 from a fresh stage (post-`133020a`/`8a3910e`/
`36f6f24`/`cfa25fb`/`35e6ce0`):**

| Target | Total | Passed | Failed | Failing tests |
|---|---|---|---|---|
| kind10 | 1022 | 1017 | 5 | blacs_pinfo, pdatrmv, pzagemv, pzahemv, pzatrmv |
| kind16 | 1045 | 1040 | 5 | (same five) — kind16 has 1045 tests because MUMPS overrides exist for kind16 only, so MUMPS tests built; kind10/multifloats staged with `--libraries` excluding mumps |
| multifloats | 1022 | 1017 | 5 | (same five) |

The same five tests fail across all three targets — portable bug,
not target-specific. **All five are undocumented** (added in commits
`5200214` and `1a7566d` between 2026-04-26 and 2026-04-29; never
ctest'd full-suite until this audit). Tracked as audit follow-up
tasks #21 (pblas aux-norm family — pdatrmv L/T at n=80, plus all
combos of pzagemv/pzahemv/pzatrmv) and #22 (blacs_pinfo
`get_broadcast_topology` subtest only).

### 3.4 `tests/RESULT.md` coverage

Per-routine digit-of-agreement table, generated by
`scripts/result_report.py`. The "BLAS" section has 75 routine rows;
0 of them have actual driver data populated — all `—` across kind10
/ kind16 / multifloats columns. This reflects the design choice to
exercise BLAS only transitively through LAPACK/PBLAS callers; it's
documented in the file's preamble.

LAPACK / ScaLAPACK / PBLAS / etc. rows have actual data populated
where drivers exist.

### 3.5 Sandbox caveats

- **`tests/blacs/TODO.md`** notes that the test sandbox runs each
  rank with a 1×1 process grid (NPROCS=1) — inter-rank communication
  is therefore not exercised. Tests are correct but coverage is
  asymmetric. Mitigation: `libmpiseq` (commits `b7ddb73`, `db54728`)
  provides a sequential-MPI stub that exercises serial paths
  cleanly.
- **`tests/mumps/TODO.md`** records the same caveat for D-rank
  parity tests (rely on libmpiseq for serial execution; full multi-rank
  coverage requires a real MPI environment).

---

## 4. Build system & CI

### 4.1 CMake structure (`cmake/CMakeLists.txt`, 868 lines)

13 sections, in build order:
1. Compiler infrastructure & target_config.cmake load (lines 1-70)
2. Multifloats conditional FetchContent (lines 75-164)
3. Helper functions (`add_standard_fortran_library`,
   `add_standard_c_library`, `add_migrated_fortran_library`,
   `add_migrated_c_library`) (lines 166-435)
4. BLAS (lines 439-454)
5. XBLAS (lines 456-488)
6. BLACS (lines 490-529)
7. LAPACK (lines 531-563)
8. PBLAS-related: PTZBLAS / PBBLAS / PBLAS (lines 565-637)
9. ScaLAPACK + scalapack_c (lines 639-724)
10. MUMPS (lines 726-756)
11. libmpiseq sequential MPI stub (lines 758-795)
12. Install rules (lines 797-851)
13. Tests subdirectory (lines 853-867)

**Per-target outputs** (LIB_PREFIX is one of `q`, `e`, `m` for real
or auto-flipped to complex-prefix as needed):

```
qblas / qxblas / qblacs / qlapack / qptzblas / qpbblas / qpblas /
qscalapack / qscalapack_c / qmumps  (kind16)
eblas / exblas / eblacs / elapack / ...                 (kind10)
mblas / mxblas / mblacs / mlapack / ...                 (multifloats)
+ blas / xblas / blacs / lapack / ... (standard-precision archives,
  shared by all targets via PUBLIC link)
+ libmpiseq.a (sequential MPI stub)
+ la_constants_mf, la_xisnan_mf, multifloats_mpi (multifloats helpers)
```

**Dependency graph** (PUBLIC linkage, kind16 example):

```
qmumps -> qscalapack -> qpblas -> qpbblas -> qblacs
                    \-> qlapack -> qblas -> blas
                                   \-> lapack
                                       \-> qxblas -> qblas
```

Wired correctly per `9938731` audit.

### 4.2 `cmake/FortranCompiler.cmake` (410 lines)

Sets:
- `FORTRAN_COMPILER_FAMILY`, `FORTRAN_COMPILER_VERSION`
- `FORTRAN_MOD_VERSION` and `FORTRAN_MOD_COMPAT_TAG` — Fortran .mod
  file format version, compiler-tagged
- `FORTRAN_COMPILER_TAG` — used in archive filenames
  (`libqlapack-gfortran-13.a` etc.)

Detects: GNU gfortran, Intel ifort/ifx, LLVM Flang, Classic Flang,
NVIDIA nvfortran, NAG, Cray. Provides `fortran_install_modules()` and
`fortran_install_library()` helpers that generate Config.cmake
that fails with available-builds list if the consumer's compiler
doesn't match.

### 4.3 CI (`.github/workflows/release.yml`)

**Trigger:** git tag `v*` or manual `workflow_dispatch`.

**Jobs:** `stage` × 3 targets → `build` × 5 compilers × 3 MPI = 45
combinations (minus exclusions) → `release` (creates GitHub Release
with attached tarballs, only on tag push).

**Compilers covered:** gfortran-12, gfortran-15, ifx-2024, ifx-2025,
flang-20.

**Exclusions:** kind10 + ifx (no 80-bit on x86-64 Linux); kind16 +
flang (no KIND=16 yet).

**Critical gap:** the staged artifact does not include `tests/`.
**ctest is never run in CI.** The pipeline produces release tarballs
of compiled libraries but performs no verification beyond linker
success.

### 4.4 Build risks

- Global `-w` (suppress all warnings) — masks potential migration
  bugs. Mitigated by tests, but the tests don't run in CI.
- **Migrator can lengthen tokens** (e.g. `MPI_DOUBLE_COMPLEX` →
  `MPI_C_LONG_DOUBLE_COMPLEX` for kind10), pushing fixed-form `.F`
  lines past column 72. Mitigated 2026-04-30 (commit `c063db3`) by
  adding `-ffixed-line-length-none` (gfortran/flang) and
  `-extend-source` (Intel) to all migrated Fortran archives via
  `add_migrated_fortran_library`. Worth flagging because future
  Fortran families (NAG, Cray, NVHPC) may need their own equivalents
  added there.
- FetchContent multifloats patches `multifloats/CMakeLists.txt` on
  the fly to handle `CMAKE_SOURCE_DIR` vs `PROJECT_SOURCE_DIR`
  mismatch. Fragile if upstream restructures.
- One observed transient build error during this session
  (`/tmp/stg-mf-fix` ld LTO plugin warning + `Error 2` on first
  multifloats build, succeeded on retry). Not reproducible; likely
  parallel-make race.

---

## 5. Documentation accuracy

### 5.1 Per-file freshness

| File | Last commit | Verdict |
|---|---|---|
| `README.md` | `823e838` (today) | FRESH |
| `doc/USAGE.md` | `823e838` (today) | FRESH |
| `doc/RECIPES.md` | recent | FRESH — does not document the abandoned `pattern_renames`; correct |
| `doc/UPSTREAM_BUGS.md` | recent | FRESH — 1 active bug, correctly documented |
| `doc/DEVELOPER.md` | recent | FRESH |
| `doc/DIVERGENCE.md` | `823e838` (today) | FRESH |
| `doc/NOTE.md` | recent | FRESH |
| `doc/PROCEDURES.md` | recent (`d5a4f48`) | **STALE** — prefix-map table at lines 19-26 still shows `t/v` for multifloats; should be `m/w`. Auto-generated by `tools/gen_procedures.py`; needs regen. |
| `doc/INTRINSICS.md` | older | STALE-ish — manual table not regenerated from `intrinsics.py`. Reference-only; low risk. |
| `doc/ARCHITECTURE.md` | older | STALE — pre-dates MUMPS/XBLAS additions and the libmpiseq path. |
| `doc/projects/test-todo-drain.md` | `823e838` (today, partial) | FRESH for UB-* status; the `Q/E/M`/`X/Y/W` prefix list was fixed today |

### 5.2 Stale-doc summary

After `823e838`:

- 8 stale spots from the previous T/V → M/W audit are fixed.
- 1 outstanding: **`doc/PROCEDURES.md` prefix table** (lines 19-26).
  Re-run `tools/gen_procedures.py` against the current staging trees
  to refresh.
- `doc/INTRINSICS.md` and `doc/ARCHITECTURE.md` are aging but not
  contradicted by code; lower priority.

### 5.3 README/USAGE accuracy

Cross-checked README.md against current state: prefix table (line 14)
shows `m`/`w` for multifloats ✓ (post-`823e838`); external deps
list (LAPACK 3.12.1, MUMPS 5.8.2, ScaLAPACK 2.2.3, XBLAS 1.0.248,
Intel MPI headers, multifloats, multifloats-mpi) is accurate; CLI
command list (`stage`, `migrate`, `build`, `run`, `verify`,
`diverge`, `converge`) matches `__main__.py` argparse. License note
"unlicensed for now" — accurate.

---

## 6. Branch state

### 6.1 Local branches (sorted by last commit)

| Branch | Last commit | Age | Status |
|---|---|---|---|
| `main` | febb49c (2026-04-24) | 6 d | Behind develop (release checkpoint) |
| `rename` | 556a335 (2026-04-25) | 5 d | Merged into develop |
| `fix/ub-01-F-glob` … `fix/ub-06-reflapack-F-glob` | (2026-04-27) | 3 d | All 6 merged |
| `audit` | 6db2292 (2026-04-28) | 2 d | Active; ScaLAPACK Phase 47 work |
| `develop` | 8e2b473 (2026-04-29) | 1 d | Ahead of `origin/develop` |
| `xblas` | d8c0508 (2026-04-29) | 1 d | Merged into develop |
| `mumps` | 0b92df6 (2026-04-30) | today | merged into tests |
| `doc` | 0b92df6 (2026-04-30) | today | tracks tests |
| `tests` (HEAD) | 823e838 (2026-04-30) | today | This session's branch |

### 6.2 Remote branches with stale risk

| Branch | Last commit | Age | Notes |
|---|---|---|---|
| `origin/feature/coalesce` | 2026-04-12 | 18 d | Unmerged, no recent activity |
| `origin/feature/multifloats` | 2026-04-12 | 18 d | Pre-dates current multifloats wiring; superseded |
| `origin/feature/fuzz` | 2026-03-19 | 42 d | Likely abandoned |
| `origin/claude/mumps-external-directory-YdfVY` | unknown | n/a | Auto-generated; possibly mergeable |

### 6.3 Merge backlog

`tests` is 1 commit ahead of `origin/tests` (`823e838`,
prefix-doc refresh). `develop` is 2 commits ahead of `origin/develop`
(xblas merge + tests-into-develop merge). `main` is at a release
checkpoint (`febb49c`, 2026-04-24); the
`xblas`/`tests`/`mumps`/`9938731` chain has not yet rolled back to
main. Pushing the branches and an eventual `main`-merge is queued
work.

---

## 7. External dependencies

### 7.1 Vendored versions

| Library | Version | Source | License |
|---|---|---|---|
| LAPACK | 3.12.1 | `external/lapack-3.12.1/` (Netlib) | BSD-3 |
| ScaLAPACK | 2.2.3 | `external/scalapack-2.2.3/` (Netlib) | BSD-3 |
| MUMPS | 5.8.2 | `external/MUMPS_5.8.2/` (LIS-LAB) | CeCILL |
| XBLAS | 1.0.248 | `external/xblas-1.0.248/` (Netlib) | BSD |
| multifloats | (FetchContent) | github.com/kyungminlee/multifloats | (check upstream) |
| multifloats-mpi | (local) | `external/multifloats-mpi/` | local |
| Intel MPI headers | (Intel stock) | `external/impi-headers/` | (Intel stub) |

### 7.2 In-tree patches against upstream

| Recipe | Override | Reason |
|---|---|---|
| `recipes/blas/source_overrides/dnrm2.f90` | safe-summation variant | multifloats: Blue's-constants formula needs LA_CONSTANTS-mediated values |
| `recipes/blas/source_overrides/dznrm2.f90` | same | same |
| `recipes/scalapack/source_overrides/pdlanhs.f` | NPROW=1 fix | upstream `II = II + JB` advance missing (UPSTREAM_BUGS.md) |
| `recipes/scalapack/source_overrides/pzlanhs.f` | same | same |
| `recipes/pblas/source_overrides/pdatrmv_.c` | L,T branch ALPHA fix (commit `50adda9`) | upstream PBLAS drops user ALPHA in off-diagonal `dagemv_` call when uplo=L,trans=T/C — see Addendum 3 |
| `recipes/pblas/source_overrides/pzatrmv_.c` | same | same |
| `recipes/mumps/{mumps_memory_mod_ep,mumps_lr_stats_ep}.F` | hand-written kind16 EP module | upstream MUMPS_MEMORY_MOD's `MUMPS_REALLOC` generic collapses S/D under D→Q rename — bridge re-exports originals + adds Q/X variants |
| `recipes/mumps/kind10/{mumps_memory_mod_ep,mumps_lr_stats_ep}.F` | same, kind10 (commit `c063db3`) | E/Y prefixes, REAL(KIND=10) — see Addendum 5 |
| `recipes/mumps/multifloats/{mumps_memory_mod_ep,mumps_lr_stats_ep}.F` | same, multifloats (commit `c063db3`) | M/W prefixes, TYPE(real64x2)/TYPE(cmplx64x2); blocked from exercise by B-8 |
| `tests/lapack/reflapack/overrides/zgedmd.f90` | reference-build fp64 leak | gfortran 13 truncates `CMPLX(real, ZERO, KIND=8)` to fp64 even with `-freal-8-real-16`; rewritten as `RWORK(N+i)*W(i,j)` (no intrinsic) |

The last entry is **a reference-build patch, not a migration patch** —
worth tracking separately because it implies a latent bug-class:
upstream Netlib code that's correct at fp64 but breaks under
gfortran's `-freal-8-real-16` promotion. If more such cases surface,
they'd land alongside `zgedmd.f90`.

### 7.3 Upstream bug tracker (`doc/UPSTREAM_BUGS.md`)

| ID | Routine(s) | Workaround | Status |
|---|---|---|---|
| 1 | ScaLAPACK 2.2.3 `p?lanhs.f` | source_overrides + prefer_source pin | ACTIVE — workaround in place; **upstream not yet reported** |

Single bug. Pattern is set for future: source override + recipe pin
+ test driver in `tests/scalapack/auxiliary/test_p?lanhs.f90`.

### 7.4 Stale/abandoned files in `external/`

None detected. No `.o`/`.mod`/`.a`/`.bak` files committed.

---

## 8. Phase-22 DMD precision divergence — focused dive

The single longest-running test failure on this branch was
`test_zgedmd` and `test_zgedmdq` on kind16 and multifloats (all
layer-(a) sorted-spectrum comparisons), failing by `~4.6e-17`
(precisely fp64 ε). Earlier in this session I attempted to track
down the leak and concluded:

- The migrated `xgedmd.f90` source is clean (only `Z→X`/`D→Q` /
  `WP=real64`→`WP=16` from `zgedmd.f90`).
- Compiling `xgedmd` with `-freal-8-real-16` does not change results
  — confirms there is no missed fp64 step inside the migrator's
  output.
- Promoting `xgeev`, all of `qblas`, `qxblas`, etc. with the same
  flag is also a no-op for the diff.

**Resolution (separate session, commits `36f6f24` / `cfa25fb`):** the
fp64 step is in the **reference build**, not the target. The
upstream Netlib `zgedmd.f90` line 1001 contains:

```fortran
W(i,j) = CMPLX(RWORK(N+i),ZERO,KIND=WP)*W(i,j)
```

With `WP = real64` and gfortran 13 + `-freal-8-real-16`, `KIND=WP`
expands to `KIND=8`. The compiler then truncates the multiplication
to fp64 even though the surrounding storage is aliased to 16 bytes.
Bleeding ~1 fp64 ULP into the Rayleigh quotient causes the eigenvalue
divergence. The migrated xgedmd has explicit `KIND=16` everywhere
and is correct.

**Fix:** `tests/lapack/reflapack/overrides/zgedmd.f90` replaces the
`CMPLX(...,KIND=WP)*W(i,j)` with `RWORK(N+i)*W(i,j)` (mathematically
identical, no intrinsic). The reference build now matches the
migrated build at quad precision.

**Status:** RESOLVED on `tests` branch (`cfa25fb` marks P22
RESOLVED in `tests/lapack/TODO.md`). The `/tmp/stg-q-fix` build
captured before this commit still shows the 2 failures; re-stage
will clear them.

**Generalization:** any future "the migrator's source is clean but
the test fails at fp64 precision" report should suspect the same
class of issue and look at the reference path, not the target.

---

## 9. Open / actionable items

Sorted by ROI (impact × inverse-effort):

1. ~~**Re-stage all three targets and re-run ctest**~~ — DONE
   2026-04-30. Confirmed kind10 1017/1022, kind16 1040/1045,
   multifloats 1017/1022; L5 / P6/P7 / P22 fixes flowed through.
   Five new portable failures discovered (see audit Section 3.3
   and follow-up tasks #21, #22).
2. ~~**Refresh `doc/PROCEDURES.md` prefix table**~~ — DONE in
   commit `35e6ce0` (gen_procedures.py M/W fix + regen against
   fresh staging trees).
3. ~~**Push branches to origin**~~ — DONE: `audit` branch pushed.
4. ~~**Resolve multifloats `gesvj` Infinity (L5)**~~ — DONE prior
   to this audit (commits `133020a` / `8a3910e`); the audit
   captured stale ctest data. Verified PASS on fresh stage
   2026-04-30.
5. ~~**Add ctest to CI**~~ — DONE in commit `35e6ce0`. kind10 +
   gfortran-15 + openmpi smoke test wired before the `release`
   job; expand once known-failures resolve.
6. ~~**Delete or rebase abandoned `feature/*` branches**~~ — DONE:
   `feature/coalesce` + `feature/multifloats` deleted from origin
   (fully merged); `feature/fuzz` already absent upstream.
7. **File upstream bug for ScaLAPACK `p?lanhs`** —
   github.com/Reference-ScaLAPACK/scalapack/issues with reduced
   reproducer from `tests/scalapack/auxiliary/test_pdlanhs.f90`.
   Effort: 30 min.
8. **MUMPS B-4, B-7, G-2** — PARTIAL (commit `c063db3`, 2026-04-30):
   kind10 EP overrides + `target_kind10` wrapper landed; `emumps`
   archive builds clean. Multifloats overrides authored but build
   blocked by new B-8 (engine bug). Test execution blocked by new
   B-9 (C-bridge hardcoded to kind16). Tracked in
   `tests/mumps/TODO.md`.
9. ~~**Refresh `doc/ARCHITECTURE.md`**~~ — DONE in commit
   `35e6ce0`. Rewrote from a 44-line skeleton to ~370 lines covering
   all 12 modules, 11 recipes, 3 targets, the C migration path, the
   classifier's position-agnostic family discovery, libmpiseq, the
   reflapack/overrides escape hatch, and design principles.
10. ~~**Update `doc/INTRINSICS.md`**~~ — DONE in commit `bc848ac`:
    header note pointing to `src/pyengine/intrinsics.py`,
    kind10/multifloats coverage section, and a cross-reference
    appendix to the migrator's rewrite table.

---

## 10. Recommendations

### Short-term (this week)

- Re-stage + re-ctest all three targets to confirm zero failures
  beyond known L5 multifloats-gesvj (item 1).
- Regenerate `doc/PROCEDURES.md` (item 2).
- Push branches (item 3).
- Add a 1-target × 1-compiler × 1-MPI ctest job to
  `.github/workflows/release.yml` (item 5, narrow scope).

### Medium-term (this quarter)

- Investigate L5 multifloats-gesvj Infinity (item 4).
- File and track upstream `p?lanhs` bug (item 7).
- Land MUMPS B-4 / B-7 / G-2 (item 8) — kind10 share landed
  2026-04-30; remaining work (multifloats build via B-8 engine fix +
  C-bridge generalization via B-9) completes MUMPS coverage on
  kind10 / multifloats.
- Decide fate of stale `feature/*` branches (item 6).

### Long-term (architecture)

- **Reference-side override mechanism**: the
  `tests/lapack/reflapack/overrides/` pattern (used now for one
  routine, `zgedmd.f90`) is likely to grow as more
  `-freal-8-real-16`-incompatible upstream constructs surface.
  Consider promoting it to a documented build-system feature
  (currently lives only in `tests/lapack/reflapack/CMakeLists.txt`
  glob), perhaps mirrored in `tests/scalapack/reflapack/` if a
  similar issue surfaces there.
- **Pyengine `__main__.py` decomposition**: at 1148 LOC with two
  >200-line functions (`_generate_cmake`, `cmd_stage`), a future
  cleanup could split CMake-template generation into its own module.
  Not urgent.
- **Auto-derived docs**: `doc/PROCEDURES.md` already has a
  generator (`tools/gen_procedures.py`); consider adding a
  `make docs` target or pre-commit hook so the table stays in sync
  with staged reality.
- **CI test run**: the absence of ctest in CI is the single
  largest verification gap. Even a 5-minute smoke job
  (`gfortran-15 + OpenMPI + kind16`, ctest -j -R "blas|lapack_test_dgesv|lapack_test_dsyev|scalapack_test_pdgesv|scalapack_test_pdsyev"`)
  would catch the most likely regressions.

---

## Appendix A — Inventory snapshot

### A.1 Top-level layout

```
fortran-migrator/
├── src/pyengine/         # 11 modules, 9039 LOC (engine)
├── recipes/              # 11 YAML + 7 sub-dirs
├── targets/              # 3 YAML
├── tests/                # 10 suites, 1051 .f90 + 3 .c
├── external/             # 7 vendored trees
├── cmake/                # CMakeLists.txt (868) + FortranCompiler.cmake (410)
├── doc/                  # 11 primary md + archive/ + projects/
├── scripts/              # 7 bash + 4 python
├── tools/                # 1 python (gen_procedures.py)
├── .github/workflows/    # release.yml only
├── pyproject.toml        # uv-managed; pyyaml, tqdm, fypp(dev), pytest(dev)
└── README.md
```

### A.2 Engine modules (`src/pyengine/`)

| File | LOC |
|---|---|
| `__init__.py` | 2 |
| `__main__.py` | 1148 |
| `c_migrator.py` | 1648 |
| `config.py` | 272 |
| `flang_parser.py` | 360 |
| `fortran_migrator.py` | 2842 |
| `gfortran_parser.py` | 350 |
| `intrinsics.py` | 352 |
| `pipeline.py` | 1365 |
| `prefix_classifier.py` | 372 |
| `symbol_scanner.py` | 175 |
| `target_mode.py` | 153 |
| **Total** | **9039** |

### A.3 Recipes

```
recipes/
├── README.md
├── blas.yaml            blas/ (source_overrides/{dnrm2,dznrm2}.f90)
├── blacs.yaml           blacs/ (mfc_overrides/ — 4 files)
├── lapack.yaml          lapack/ (extras/ — 3 files; mf_helpers/ — 2 files)
├── mumps.yaml           mumps/ (keep_kind manifest, helpers)
├── pblas.yaml           pblas/ (mfc_overrides/ — 3 files)
├── pbblas.yaml
├── ptzblas.yaml
├── scalapack.yaml       scalapack/ (extras/ — 2; source_overrides/ — 2; mfc_overrides/ — 1)
├── scalapack_c.yaml
├── xblas.yaml           xblas/ (gen_skip_list.py — auto-generated 402-entry skip list)
```

### A.4 Tests

```
tests/
├── blas/        76 .f90, 0 drivers (transitive coverage only)
├── lapack/      611 .f90, ~130 drivers, TODO.md (P22 RESOLVED on tests/, L5 OPEN, P17xx BLOCKED)
├── scalapack/   171 .f90, TODO.md (banded DELIVERED, doc-update only)
├── pblas/       62 .f90, TODO.md (resolved; coverage footnote in doc/PROCEDURES)
├── pbblas/      9 .f90, TODO.md (smoke-only)
├── blacs/       22 .f90, TODO.md (rank-1 sandbox caveat)
├── ptzblas/     35 .f90, TODO.md (mixed-prec ZZDOTC/ZZDOTU)
├── xblas/       44 .f90, TODO.md (43/43 multifloats — commit 42126e1)
├── mumps/       21 .f90 + 3 .c, TODO.md (active; B-4/B-7/G-2 OPEN)
└── common/      0 drivers (helper modules, common/ref_quad_xblas.f90 etc.)
```

### A.5 Vendored externals

```
external/
├── lapack-3.12.1/        Netlib, BSD-3, 2024-05-31 release
├── scalapack-2.2.3/      Netlib, BSD-3, 2023-01-13 release
├── MUMPS_5.8.2/          LIS-LAB, CeCILL, 2024-05-27 release; libseq for sequential MPI stub
├── xblas-1.0.248/        Netlib, BSD, ~2014; added 2026-04-29
├── multifloats.hh        single-header bridge
├── multifloats-mpi/      local C++ MPI bridge
└── impi-headers/         Intel MPI compilation stubs (no source)
```

### A.6 CI matrix

```
release.yml: 3 stage × 5 compilers × 3 MPI = 45 build jobs
   targets:    kind10  | kind16  | multifloats
   compilers:  gfortran-12 / gfortran-15 / ifx-2024 / ifx-2025 / flang-20
   MPI:        OpenMPI / MPICH / Intel MPI
   exclusions: kind10+ifx (no 80-bit); kind16+flang (no KIND=16)
   ctest:      NOT RUN
```

---

*End of audit. Compiled 2026-04-30 from HEAD `823e838`.*

---

## Addendum (2026-04-30, after audit follow-up work)

After the audit was committed (`7ffd914`), six of the audit's
actionable items were closed in two follow-up commits:

- `35e6ce0` — `tools/gen_procedures.py` PREFIXES['multifloats']
  changed from `t/v/tv/vt/it/iv` to `m/w/mw/wm/im/iw`,
  `doc/PROCEDURES.md` regenerated, `doc/ARCHITECTURE.md` rewritten,
  CI `test` job added (kind10 + gfortran-15 + openmpi smoke).

A fresh stage of all three targets against the current HEAD
(`audit` branch HEAD = `35e6ce0`, ahead of `tests` HEAD
`823e838`) confirmed:

- L5 (`gesvj` Infinity) — RESOLVED ✓
- P6/P7 (`?gesvdq` workspace overrun) — RESOLVED ✓
- P22 (DMD fp64 leak) — RESOLVED ✓

Five new test failures emerged (consistent across all three
targets, so the bug is portable):

| Test | Subtest / pattern | Likely cause |
|---|---|---|
| `blacs_test_blacs_pinfo` | `get_broadcast_topology` only | Single-rank sandbox can't really exercise broadcast topology — but the test is checked in not-skip'd |
| `pblas_test_pdatrmv` | `uplo=L,trans=T,n=80` (both `diag=N`/`U`); other combos PASS at ~33 digits | Likely off-by-one or wrong-triangle bug in migrated pdatrmv `L,T` path |
| `pblas_test_pzagemv` | All combos, ~0.7 digits | Systematic — possibly wrong reference (missing `|conjg(z)|=|z|` factor?) or wrong-target migration |
| `pblas_test_pzahemv` | All combos, ~0.7 digits | Same family as above |
| `pblas_test_pzatrmv` | All combos, ~0.7 digits | Same family as above |

All five tests were added in `5200214` (2026-04-26 — pblas aux-norm
coverage) and `1a7566d` (2026-04-29 — blacs_pinfo probes), and were
never ctest'd full-suite at the time of merge. Tracked as audit
follow-up tasks #21 (pblas aux-norm cluster) and #22 (blacs_pinfo).
These are real OPEN issues; they should be added to
`tests/pblas/TODO.md` and `tests/blacs/TODO.md` respectively.

Snapshot pass rates (kind10/kind16/multifloats: all 99.5%) remain
the headline number; the prior audit's claim of "kind10: 728/728"
was based on a partial test build, where MUMPS errors had
short-circuited the LAPACK/PBLAS test compilation.

The remaining audit items (item 6 stale-branch cleanup, item 7
upstream `p?lanhs` bug filing, item 8 MUMPS B-4/B-7/G-2 kind10/
multifloats overrides, item 10 INTRINSICS.md regen) are deferred —
they require user-triaged decisions or substantial additional
work.

— end of addendum —

---

## Addendum 2 (2026-04-30, post-investigation)

The five test failures from Addendum 1 were investigated; three turned
out to be **test-driver bugs**, not migration bugs (commit `4b2d5ee`):

- `blacs_test_blacs_pinfo`: the test treated `BLACS_GET WHAT=10` as
  "broadcast topology" but it's actually `SGET_BLACSCONTXT` (returns
  the context's MPI Fortran handle). BLACS has no GET-able topology;
  it's a one-character argument or set via `BLACS_SET WHAT=0`. Subtest
  renamed `get_blacs_context_handle` with a sane non-zero check.

- `pblas_test_pzagemv`, `pblas_test_pzahemv`, `pblas_test_pzatrmv`:
  the test references used Euclidean `abs(complex)`, but the routines
  use CABS1 = `|Re| + |Im|` (Manhattan). For pzahemv specifically,
  the routine treats the diagonal as `|Re(A(j,j))|` only because of
  the Hermitian invariant. References rewritten.

After the fix, the three pz* tests pass. pzagemv and pzahemv at all
combos; pzatrmv at all combos except the (L,T,n=80) and (L,C,n=80)
group — which still fails with the same magnitude as
`pblas_test_pdatrmv` at (L,T,n=80). Both surface only at n=80 with
mb=nb=8 in a single-rank distribution, suggesting a parallel-
distribution / block-boundary bug in the migrated PDATRMV / PZATRMV
or in their shared `PB_C*` helpers. Tracked as audit follow-up task
#23 for both.

Final ctest pass rates (excluding the still-investigating
`test_pzheevr` hang, task #24):

| Target | Total | Passed | Failed | Pass rate |
|---|---|---|---|---|
| kind10 | 1021 | 1019 | 2 | 99.80% |
| kind16 | 1044 | 1042 | 2 | 99.81% |
| multifloats | 1021 | 1019 | 2 | 99.80% |

Two remaining failures across all three targets:
`pblas_test_pdatrmv` and `pblas_test_pzatrmv` at the L,T,n=80 case.
Same root cause — a single bug in patrmv's L,T branch at multi-block-row
distributions.

A side discovery: `scalapack_test_pzheevr` hangs indefinitely
(rank 0 spinning at 100% CPU, ranks 1-3 stuck in MPI wait). Likely a
sandbox-MPI artifact, but the test was running for >2 hours before
being killed. Tracked as task #24; for now excluded from ctest via
`-E "test_pzheevr"`.

— end of addendum 2 —

---

## Addendum 3 (2026-04-30, 100% pass)

The two remaining failures from Addendum 2 (`pblas_test_pdatrmv` and
`pblas_test_pzatrmv` at uplo=L,trans=T,n=80) were traced to a single
**upstream ScaLAPACK PBLAS bug** in `pdatrmv_.c` line 573 and
`pzatrmv_.c` line 577: the off-diagonal `dagemv_`/`zagemv_` call in
the (uplo='L', trans='T'/'C') branch only passes `one` instead of the
user-supplied `ALPHA`. Every other (uplo,trans) combination uses
ALPHA correctly. Result:

    y_got = y_ref + (1 - alpha) * |L_below^T * x_below|

silently incorrect when `alpha != 1.0`. The test catches it at n=80
(10 block-rows of mb=8) with `alpha = 0.6`; n=40 masks because the
diagonal-block contribution dominates when the block count is
small.

**Fix (commit `50adda9`):**

- Patched copies in `recipes/pblas/source_overrides/{pdatrmv_,pzatrmv_}.c`
  with the single-token swap `one → ((char *) ALPHA)`.
- New `source_overrides:` block in `recipes/pblas.yaml` to wire them in.
- Extended the C migrator (`c_migrator.py` + `pipeline.py`) to honor
  `source_overrides` like the Fortran path does. Previously C-language
  recipes silently ignored `source_overrides` (the field documentation
  in `config.py:165` was Fortran-flavored). Now both languages use the
  same recipe field — closes a doc/code gap.

**Final ctest pass rates** (excluding the unrelated `test_pzheevr`
MPI-sandbox hang, audit task #24):

| Target | Total | Passed | Failed | Pass rate |
|---|---|---|---|---|
| kind10 | 1021 | 1021 | 0 | **100.00%** |
| kind16 | 1044 | 1044 | 0 | **100.00%** |
| multifloats | 1021 | 1021 | 0 | **100.00%** |

All three targets are now ctest-clean. Audit follow-up tasks #16-#23
are all closed; only #24 (pzheevr MPI hang, environmental) remains
open in this session's scope.

The fix should be filed upstream to ScaLAPACK
(github.com/Reference-ScaLAPACK/scalapack/issues) — same workflow as
the existing `p?lanhs.f` NPROW=1 bug in `doc/UPSTREAM_BUGS.md`.
Patch is small enough (one-token swap on two files) that an upstream
PR is straightforward.

— end of addendum 3 —

---

## Addendum 4 (2026-04-30, pzheevr root cause)

The `scalapack_test_pzheevr` hang/crash (audit task #24) was investigated.
Root cause is **Intel MPI 2021.17 (and likely earlier) does not properly
implement `MPI_SUM` on `MPI_REAL16`**:

```fortran
real(kind=16) :: x, y
call MPI_Allreduce(x, y, 1, MPI_REAL16, MPI_SUM, MPI_COMM_WORLD, ierr)
```

segfaults inside `MPIR_SUM at .../coll/op/opsum.c:36` for any number of
ranks > 1. Switching `MPI_REAL16 → MPI_LONG_DOUBLE` doesn't crash but
returns nonsense (`sum = 1.0` for input `1+2+3+4 = 10`). MPICH's
`/usr/bin/mpiexec` doesn't crash either but appears to hang at 100% CPU
on rank 0 (observed earlier) — a different failure mode for the same
underlying gap.

The migrator's BLACS `qgsum2d_.c` line 156 uses
`bp->dtype = MPI_REAL16; ... MPI_Allreduce(..., MPI_SUM, ...)`. This
pattern is used by every kind16 BLACS reduction wrapper (qgsum2d_,
qgamn2d_, qgamx2d_, qgesd2d_, qgerv2d_, qgebs2d_, qgebr2d_, qtrbs2d_,
qtrbr2d_, qtrsd2d_, qtrrv2d_) and by symmetric paths for the complex
prefix `x` (xgsum2d_, etc.).

The right fix is to mirror the **multifloats** approach: register
custom `MPI_Op`s (analogous to `MPI_DD_SUM` / `MPI_ZZ_SUM` /
`MPI_DD_AMX` / `MPI_DD_AMN` etc., per
`external/multifloats-mpi/multifloats_mpi.cpp:82-87`) for kind16, then
swap `MPI_SUM` references in BLACS to those. Multifloats avoids the
Intel-MPI bug entirely because its custom MPI_Op_create-based
operators do their own type-aware reduction in user code.

Estimated effort: a couple of days. ~12 BLACS files per real/complex
prefix per target, plus one new `quad_mpi.cpp`-style helper module
mirroring `multifloats_mpi.cpp`, plus CMake wiring. Out of scope for
this audit; tracked as **task #24** for follow-up.

In the meantime, the CI `test` job and any local ctest invocation
should keep `-E "test_pzheevr"` (already in
`.github/workflows/release.yml`'s test-job snippet) so the broken
kind16 BLACS reduction path doesn't deadlock or crash. The same
pattern probably affects multi-rank `pdsyevr` / `pzheevx` /
`pdsyevx` variants on kind16 — worth surveying when the proper fix
lands.

— end of addendum 4 —

---

## Addendum 5 (2026-04-30, MUMPS B-4 partial — kind10 unblocked)

Audit item #8 (MUMPS B-4 / B-7 / G-2) was advanced from OPEN to
PARTIAL in commit `c063db3`.

**Done:**

- **kind10 EP overrides authored** —
  `recipes/mumps/kind10/{mumps_memory_mod_ep.F,mumps_lr_stats_ep.F}`
  (REAL(KIND=10) reallocators with E/Y prefixes; LR-stats DP-cast
  bridge). Recipe wired via new per-target `overrides:` stanzas.
- **multifloats EP overrides authored** —
  `recipes/mumps/multifloats/{...}.F` with `TYPE(real64x2)` /
  `TYPE(cmplx64x2)` and M/W prefixes. Files are correct; build is
  blocked separately (see B-8 below).
- **`emumps` archive builds clean** for the first time on a full
  `pyengine stage --target kind10` + cmake build (with Intel MPI,
  per the project's canonical MPI environment).
- **Latent fixed-form line-length bug fixed** — kind10's
  `MPI_DOUBLE_COMPLEX` → `MPI_C_LONG_DOUBLE_COMPLEX` rewrite (a 7-char
  expansion) pushed several MUMPS `.F` lines past column 72.
  `add_migrated_fortran_library` in `cmake/CMakeLists.txt` now adds
  `-ffixed-line-length-none` (gfortran/flang) / `-extend-source`
  (Intel) so all migrated Fortran archives are immune to migrator
  token-growth in fixed-form sources.
- **`tests/mumps/target_kind10/target_mumps.fypp`** wrapper landed
  (G-2 partial) so the suite picks up the kind10 target when the
  C-bridge generalization (B-9) lands.

**New blockers surfaced (added to section 3.2 phase tracker):**

- **MUMPS B-8 — multifloats `USE multifloats` injector mishandles
  CPP-interspersed continuation in SUBROUTINE headers** (engine bug).
  `mana_aux.F:DMUMPS_ANA_F` and similar files conditionalize METIS
  arguments inline via `#if defined(metis) ... & , METIS_OPTIONS ...
  #endif` inside the formal arg list. The multifloats post-pass
  treats the first non-continued line ending in something other than
  `)` as the SUBROUTINE terminator and inserts `USE multifloats`
  there — splitting the formal arg list. Fully blocks `mmumps`
  build. Engine fix needed in `fortran_migrator.py`'s multifloats
  USE-injection path: treat CPP `#if/#endif` blocks as transparent
  for the end-of-statement search, or use a Fortran-aware parser at
  the injection site.

- **MUMPS B-9 — C-bridge + test infrastructure hardcoded to kind16**.
  `tests/mumps/CMakeLists.txt` hardcodes `qmumps_c_bridge` /
  `xmumps_c_bridge`, the `__float128`-typed `mumps_c_types.h` shadow,
  and the `dmumps_c=qmumps_c` define stack to kind16. Even the
  Fortran-only mumps tests pull the bridge through the shared
  `_mumps_link_group_archives` LINK_GROUP, so the kind10 `emumps`
  archive can build but its tests don't run. Generalization needs
  per-target headers (`emumps_c.h` with `long double`), prefix
  parameterization of bridge object-libs, and either a split between
  Fortran-only and bridge-bearing test executables or full per-target
  bridge plumbing.

**Status updates flowed into section 3.2:**

- B-4: PARTIAL (kind10 done; multifloats blocked on B-8; sanity
  checks still TODO)
- B-7: PARTIAL (same TODO entry)
- G-2: PARTIAL (kind10 wrapper done; multifloats wrapper deferred)
- B-8, B-9: NEW, OPEN

**Remaining audit-actionable work after this commit:**

- B-8 — multifloats USE-injector engine fix (most strategically
  valuable next step; unblocks both `mmumps` and the rest of B-4)
- B-9 — C-bridge generalization (lets kind10 `emumps` actually be
  exercised by ctest)
- Custom MPI_Op for kind16 BLACS reductions (Addendum 4 / task #24,
  multi-day) — closes `test_pzheevr` exclusion
- Smaller B-4 leftovers: multifloats `mmumps` build (via B-8) +
  `target_multifloats/target_mumps.fypp` wrapper + `INFOG(20)`
  byte-width sanity check across kind10/mf vs kind16

— end of addendum 5 —
